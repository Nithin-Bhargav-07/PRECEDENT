/** Backend API endpoint definitions. */

export const API_ENDPOINTS = {
  extractFactors: "/api/v1/extract-factors",
  evaluatePrecedent: "/api/v1/evaluate-precedent",
  cases: "/api/v1/cases",
  sessions: "/api/v1/sessions",
} as const;
